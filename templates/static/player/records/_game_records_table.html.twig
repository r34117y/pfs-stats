<div class="box" x-data="gameRecordsTableData({{ playerId }}, '{{ endpoint }}')" x-init="fetchRows()">
    <h2 class="title is-5">{{ title }}</h2>

    <template x-if="loading">
        <p>Ładowanie...</p>
    </template>

    <template x-if="error">
        <article class="message is-danger">
            <div class="message-body" x-text="error"></div>
        </article>
    </template>

    <div x-show="!loading && !error">
        <table class="table is-bordered is-hoverable is-fullwidth">
            <thead>
            <tr>
                <th>#</th>
                <th>Punkty</th>
                <th>Przeciwnik</th>
                <th>Wynik</th>
                <th>Turniej</th>
            </tr>
            </thead>
            <tbody>
            <template x-for="row in rows" :key="row.position">
                <tr>
                    <td class="text-center" x-text="row.position"></td>
                    <td class="text-center" x-text="row.points"></td>
                    <td x-text="row.opponent"></td>
                    <td class="text-center" x-text="row.score"></td>
                    <td x-text="row.tournament"></td>
                </tr>
            </template>
            <template x-if="!rows.length">
                <tr>
                    <td colspan="5" class="text-center">Brak danych</td>
                </tr>
            </template>
            </tbody>
        </table>
    </div>
</div>
